# Phase 1 (exploration) on the MOCK env needs NOTHING beyond the Python stdlib
# (python3 -m goexplore.run --env mock ...).
#
# The REAL engine (--env native) + training is NOT a pip install. It builds
# PufferLib 4.0 (Ocean) + the liujonathan24/NetHack fork from source. Use:
#
#     scripts/build_native.sh      # apt deps, CUDA toolkit, clone, build everything
#     source scripts/env.sh        # NETHACKDIR / LD_LIBRARY_PATH / venv
#
# That installs (into PufferLib's venv): torch>=2.9 (cuda), numpy, wandb, pybind11,
# etc. — see PufferLib's pyproject.toml. There is no `pip install nle`; the fork
# is a pure-C engine linked into libnethack.so / pufferlib/_C.so.
